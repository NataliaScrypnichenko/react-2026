зауваження вчителя 70
Немає жодного коментаря біля створення axiosInstance - не пояснено, навіщо окремий інстанс з фіксованим baseURL.
- Не пояснено, чому токен підставляється саме в GET-запити, а не в усі (це найважливіший момент інтерцептора).
- loadAuthProducts: подвійна деструктуризація {data: {products}} фактично не розписана - коментар обірваний на середині думки.
- routes.tsx, main.tsx, App.tsx, MainLayout.tsx - взагалі без пояснень.
- Моделі IProduct і IBaseResponseModelType не прокоментовані.
- Не відзначено, що дані логіна захардкоджені (тестовий юзер), а не з форми.
- Не пояснено послідовність .then().then() як два кроки підряд, а не паралельно.

і тд...

зауваження виправила в уроці lessons/Auth-6-4.2
